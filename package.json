{
  "name": "ormezo-parking",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "node -e \"const fs=require('fs'); const {execSync}=require('child_process'); try{fs.writeFileSync('.commit', execSync('git rev-parse --short HEAD').toString().trim())}catch(e){fs.writeFileSync('.commit','<no-git>')} console.log('BUILD_COMMIT:', fs.readFileSync('.commit','utf8'))\""
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "dotenv": "^17.2.3"
  }
}
